# RailSmart Frontend Environment (EXAMPLE)
# Copy to `frontend/.env` if you want to provide Razorpay key_id via Vite.
# Note: app also fetches key_id from backend at /api/payment/key.

VITE_RAZORPAY_KEY_ID=rzp_test_your_key_id_here
